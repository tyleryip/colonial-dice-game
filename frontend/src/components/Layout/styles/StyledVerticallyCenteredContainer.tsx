import { styled } from 'styled-components'

/**
 * Used to center main content of the app vertically
 */
const StyledVerticallyCenteredContainer = styled.div`
    margin-top: auto;
    margin-bottom: auto;
    width: 100%;
`

export default StyledVerticallyCenteredContainer