import { styled } from "styled-components"

/**
 * Any icon, graphic, or image that represents a game piece
 */
const StyledAsset = styled.img`
    position: absolute;
    width: 100%;
`

export default StyledAsset